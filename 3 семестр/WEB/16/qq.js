function Mathem(q){
	let n = Math.sqrt(q);
	alert(n);
}

function R(){
	let r = Math.round(n.value);
	n.value = r;
}

function Rand(){
	let r = Math.random(q.value);
	q.value = r;
}

function sq(){
	let r = Math.sqrt(w.value);
	w.value = r;
}

function po(){
	let r = Math.pow(p.value, 2);
	p.value = r;
}

function M(){
	let r = Math.max(1,3,2,4);
	alert("Максимальное число = " 	+ r);
}