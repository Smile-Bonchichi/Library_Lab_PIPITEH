#include <iostream>
#include <ctime>
#include <fstream>
#include <string>
#include "Windows.h"
#include <time.h>
#include <iomanip>

#define ll long long
#define str string

using namespace std;

class Rect {
	double length;
	double width;
public:
	Rect(double l, double w) {
		this->length = l;
		this->width = w;
	}
	Rect(double square) {
		this->length = this->width = square;
	}
	void output() {
		cout << "Length = " << length << "\tWidth = " << width << endl;
	}
	double rect_area() {
		return (length * width);
	}
	/*double rect_area(double length, double width) {
		return (length * width);
	}
	double rect_area(double square) {
		return (square * square);
	}*/
};

int main() {
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);

	Rect a(5), b(1, 4);
	a.output();
	b.output();
	Rect c = a.rect_area();
	cout << "a.rect_area = " << a.rect_area() << endl;
	c.output();
	return 0;
}